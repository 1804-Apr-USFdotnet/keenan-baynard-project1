@model IEnumerable<RestaurantModels.Restaurant>


<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>search</title>
</head>
<body>
    @for (int i = 0; i < Model.Count() - 1;)
    {
        <div class="row">
            <div class="col-sm-4">
                <b>@Model.ElementAt(i).Name</b>
                <div>@Model.ElementAt(i).City,  @Model.ElementAt(i++).State</div>
            </div>
            <div class="col-sm-4">
                <b>@Model.ElementAt(i).Name</b>
                <table>
                    <tbody>
                        <tr>@Model.ElementAt(i).City,  @Model.ElementAt(i++).State</tr>
                    </tbody>
                </table>
            </div>
            <div class="col-sm-4">
                <b>@Model.ElementAt(i).Name</b>
                <table>
                    <tbody>
                        <tr>@Model.ElementAt(i).City,  @Model.ElementAt(i++).State</tr>
                    </tbody>
                </table>
            </div>
        </div>
    }


</body>
</html>
